# iOS No-Period Mirror

This directory mirrors canonical repository paths that begin with a leading period.

## Mapping

| iOS-safe path | Canonical repository path |
| --- | --- |
| `iosnoperiod/github/workflows/rc1-validation.yml` | `github/workflows/rc1-validation.yml` |

The canonical repository path shown above is displayed without the leading period for iOS compatibility. In the repository, the workflow path begins with a leading period.
